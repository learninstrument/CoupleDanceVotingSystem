<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Love in Motion</title>
    <!-- Fonts & Bootstrap -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;800&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #421746;
            --accent-color: #a98213;
        }
        body {
            font-family: 'Poppins', sans-serif;
            /* Matches the homepage background */
            background: linear-gradient(135deg, #431948, #000000b3), url('/images/bg.png');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .card {
            border: none;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.3);
            background: rgba(255, 255, 255, 0.95); /* Slight transparency */
            backdrop-filter: blur(10px);
            overflow: hidden;
        }
        .card-header {
            background: transparent;
            border-bottom: none;
            padding-top: 30px;
            text-align: center;
        }
        h2 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 800;
            color: var(--primary-color);
            text-transform: uppercase;
            margin-top: 10px;
        }
        .form-control {
            border-radius: 10px;
            padding: 12px 15px;
            border: 1px solid #ddd;
            background-color: #f8f9fa;
            font-size: 0.9rem;
        }
        .form-control:focus {
            box-shadow: 0 0 0 3px rgba(66, 23, 70, 0.1);
            border-color: var(--primary-color);
            background-color: white;
        }
        .btn-primary {
            background: linear-gradient(to right, #421746, #a98213);
            border: none;
            border-radius: 50px;
            padding: 12px;
            font-weight: 600;
            width: 100%;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .btn-primary:hover {
            background: linear-gradient(to right, #2e0f31, #8a680f);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(66, 23, 70, 0.3);
        }
        .form-check-input:checked {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }
        .login-link {
            color: var(--primary-color);
            font-weight: 600;
            text-decoration: none;
        }
        .login-link:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card p-3 p-md-4">
                    <div class="card-header">
                        <img src="/images/logo.png" alt="Logo" height="50" class="mb-2">
                        <h2>Join the Dance</h2>
                        <p class="text-muted small">Create your account to vote or compete!</p>
                    </div>
                    <div class="card-body">
                        <form action="/register" method="POST">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold small text-muted">Username</label>
                                    <input type="text" name="username" class="form-control" placeholder="e.g. DanceKing" required>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold small text-muted">Phone Number</label>
                                    <input type="tel" name="phone_number" class="form-control" placeholder="080..." required>
                                </div>
                                <div class="col-12">
                                    <label class="form-label fw-bold small text-muted">Email Address</label>
                                    <input type="email" name="email" class="form-control" placeholder="name@example.com" required>
                                </div>
                                <div class="col-12">
                                    <label class="form-label fw-bold small text-muted">Password</label>
                                    <input type="password" name="password" class="form-control" placeholder="••••••••" required>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold small text-muted">Social Media Handle</label>
                                    <input type="text" name="social_media_handle" class="form-control" placeholder="@instagram_handle">
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold small text-muted">Referral Code (Optional)</label>
                                    <input type="text" name="referred_by" class="form-control" placeholder="Code">
                                </div>
                                
                                <div class="col-12 mt-3">
                                    <div class="form-check p-3 border rounded bg-white shadow-sm">
                                        <input class="form-check-input ms-1" type="checkbox" name="is_contestant" id="contestantCheck">
                                        <label class="form-check-label fw-bold ms-2 text-dark" for="contestantCheck">
                                            Register as a Contestant Couple?
                                        </label>
                                        <div class="small text-muted ms-4 mt-1">
                                            Registration fee: <strong style="color: var(--accent-color)">₦<%= parseInt(registrationFee).toLocaleString() %></strong>. You will be redirected to payment after verification.
                                        </div>
                                    </div>
                                </div>

                                <div class="col-12 mt-4">
                                    <button type="submit" class="btn btn-primary text-white">Create Account</button>
                                </div>
                            </div>
                        </form>
                    </div>
                    <div class="card-footer text-center bg-transparent border-0 mt-2">
                        <p class="small text-muted">Already have an account? <a href="/login" class="login-link">Login here</a></p>
                        <a href="/" class="small text-muted text-decoration-none">← Back to Home</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Error Modal -->
    <div class="modal fade" id="errorModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0 shadow">
                <div class="modal-header bg-danger text-white border-0">
                    <h5 class="modal-title fw-bold">Registration Failed</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body text-center p-4">
                    <div class="mb-2 text-danger" style="font-size: 2rem;">⚠️</div>
                    <p class="mb-0 fw-bold"><%- typeof error !== 'undefined' ? error : '' %></p>
                </div>
            </div>
        </div>
    </div>

    <% if (typeof error !== 'undefined' && error) { %>
        <script>
            document.addEventListener('DOMContentLoaded', function() {
                var errorModal = new bootstrap.Modal(document.getElementById('errorModal'));
                errorModal.show();
            });
        </script>
    <% } %>
</body>
</html>